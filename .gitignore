# Pico SDK — installed separately
pico-sdk/

# All build directories anywhere in the repo
**/build/

# Old v1 scratch files — comment this out if you want them tracked
# can_bridge/src/old_codes_from_v1/

# Leftover CMake / build artifacts if they ever appear outside build/
CMakeCache.txt
CMakeFiles/
cmake_install.cmake
Makefile
*.elf
*.uf2
*.bin
*.hex
*.map
*.dis
